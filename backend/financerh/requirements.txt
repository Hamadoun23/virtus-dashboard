Django==6.1
djangorestframework==3.18.0
djangorestframework-simplejwt==5.5.1
# Signature RS256 : SimpleJWT n'apporte que PyJWT, sans le module de
# chiffrement. Le jeton de GDA Hub est signe par une cle privee, et le
# verifier sans cryptography leve « Algorithm not supported » a la premiere
# requete. Voir accounts/hub.py.
cryptography==50.0.0
django-cors-headers==4.9.0
django-filter==26.1
python-dotenv==1.2.2
# Postgres (base Supabase locale) : inutiles tant que DATABASE_URL est vide,
# la configuration retombe alors sur SQLite.
dj-database-url==3.1.0
psycopg[binary]==3.2.12
