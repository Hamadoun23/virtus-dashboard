Django>=4.2,<5.0
djangorestframework>=3.15
django-cors-headers>=4.3
# Compte unique de GDA Hub : verification du jeton RS256 signe par identity.
# cryptography est indispensable — sans lui, PyJWT leve « Algorithm not
# supported » a la premiere requete. Voir accounts/hub.py.
PyJWT>=2.8
cryptography>=42.0
openpyxl>=3.1.0
# Photos de prospection (ImageField)
Pillow>=10.0
PyMySQL>=1.1.0

# Production
gunicorn>=22.0
whitenoise>=6.6
dj-database-url>=2.1
psycopg[binary]>=3.1
